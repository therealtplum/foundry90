# apps/web

Next.js frontend placeholder.

Suggested next steps:

- Initialize a Next.js app here (e.g. `npx create-next-app@latest .`).
- Configure it to use the API URL in `NEXT_PUBLIC_API_URL`.
- Build your dashboard and UI flows.