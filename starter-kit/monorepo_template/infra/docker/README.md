# infra/docker

Add Dockerfiles and infra notes here.

Examples:

- `web.Dockerfile` for the Next.js app
- `rust-api.Dockerfile` for the API
- `python-etl.Dockerfile` for ETL jobs

Keep infra-related configuration and notes in this area so they don’t get lost in app code.